"""
Repository implementations: versioned JSON on disk and SQLite.

Disk is the source of truth for graphs, artifacts, profiles, and
evidence; SQLite indexes runs and holds the state the UI needs to query.
"""
